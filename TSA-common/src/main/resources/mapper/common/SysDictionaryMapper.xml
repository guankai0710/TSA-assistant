<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ryan.tsa.common.mapper.SysDictionaryMapper">

    <select id="getByTypeCode" resultType="com.ryan.tsa.common.vo.SysDictionaryVo">
        SELECT
            type_code AS typeCode,
            type_name AS typeName,
            dic_value AS dicValue,
            dic_name AS dicName
        FROM
            tsa_sys_dictionary
        WHERE
            type_code = #{typeCode} AND deleted = 0
    </select>

</mapper>
